# Topic: Built-in functions — zip, enumerate, map, filter, sorted
# These are interview-frequent and used everywhere

# --- zip ---
# TODO: Zip two lists (names and scores) into a dict
names = ["Alice", "Bob", "Charlie"]
scores = [85, 92, 78]

# TODO: Unzip a list of tuples into two separate lists
pairs = [(1, 'a'), (2, 'b'), (3, 'c')]

# --- enumerate ---
# TODO: Print each item with its index using enumerate
fruits = ["apple", "banana", "cherry"]

# TODO: Use enumerate with start=1 for 1-based indexing

# --- map ---
# TODO: Use map to convert a list of strings to uppercase
words = ["hello", "world", "python"]

# TODO: Use map with a lambda to square each number
numbers = [1, 2, 3, 4, 5]

# --- filter ---
# TODO: Use filter to keep only even numbers
numbers = range(10)

# TODO: Use filter to remove empty strings from a list
strings = ["hello", "", "world", "", "python"]

# --- sorted ---
# TODO: Sort a list of dicts by a specific key
students = [{"name": "Alice", "grade": 85}, {"name": "Bob", "grade": 92}, {"name": "Charlie", "grade": 78}]

# TODO: Sort in reverse order
# TODO: Sort by multiple keys (grade desc, name asc)
